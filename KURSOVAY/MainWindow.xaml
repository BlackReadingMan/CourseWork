<Window x:Class="KURSOVAY.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:vm="clr-namespace:KURSOVAY.ViewModels"
        xmlns:local="clr-namespace:KURSOVAY"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowState="Maximized" Background="Gray">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="ContentRendered">
            <i:InvokeCommandAction Command="{Binding Window_ContentRenderedCommand}" CommandParameter="{Binding ElementName=MainGrid}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid Name = "MainGrid">
        <Image Source="{Binding WB,Mode=OneWay}"></Image>
    </Grid>
</Window>
