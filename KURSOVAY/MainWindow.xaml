<Window x:Class="CourseWork.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CourseWork.ViewModels"
        xmlns:ct="clr-namespace:CourseWork.Controls"
        mc:Ignorable="d"
        Title="КУРСОВАЯ" Height="450" Width="800" WindowState="Maximized"
        >
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <ct:Scene PaintedObj="{Binding CurrentObj, Mode=OneWay}" Settings="{Binding CurrentSettings, Mode=OneWay}" />
    </Grid>
</Window>
